{
  "name": "@mafw/sdk",
  "version": "1.0.0",
  "private": true,
  "description": "MAFW SDK — typed HTTP client and DTOs for the MAFW gateway",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
