# Tasks 任务列表面板组件 — 开发文档

> 依据截图 1:1 拆解的 Agent 任务列表面板（Task List Panel）。适用于 Agent 工作台中展示一次运行（Run）内的工具调用 / 子任务执行进度。
> 本文档覆盖：视觉结构、设计规格、状态机、交互行为、数据结构、组件 API 与 SolidJS + Tailwind 实现参考。
>
> **v2 更新（2026-08-04）**：新增**密度模型**——任务数 > 4 时列表改为三段式渲染（§2 密度结构 / §3.4 优先级徽章 / §9.1 窗口规则），解决"任务一多就把聊天区挤没"的问题；目标：任何任务量下面板总高 ≤ 40vh，且可见内容恰好是"正在做什么 + 接下来做什么"。
>
> **v2.1 更新（2026-08-04）**：行样式对齐业界共识（Claude Code / Copilot / Devin 均如此）——**状态改由行首图标表达**（空心环 / 旋转 spinner / 绿勾），移除 running/pending 文字 pill；优先级徽章从大写 pill 改为**小旗帜图标**；拖拽把手改为 hover 才显示；密度模式行高收紧到 36px。见 §3.4 / §3.5 / §4.1。
>
> **v2.2 更新（2026-08-04）**：新增**摆放形态** `placement`（§4.4）——面板从 Composer 上方迁出。原因：「任务列表不该抢占聊天区」是公认痛点，inline 浮层只是减轻遮挡、没有解决占位。
>
> **v2.3 更新（2026-08-04）**：默认形态定为**任务指示条 `bar`**（§4.4）——32px 单行渲染"当前任务 + 进度"，完整清单收进锚定 popover，📌 可钉成右侧 Dock（v2.2 的常驻 Dock 降级为 Pin 态：320px 永久税 + 空面板感）。组件内部规格（图标制、密度模型）不变；清单为单例，随形态在 popover/dock 容器间迁移。
>
> **v2.4 更新（2026-08-05）**：`bar` 形态从"独立 32px 单行"改为**嵌入宿主聊天头部行**（与 Agent title 同一行，竖分隔线后 `flex-1`）——聊天区常态成本降为 0；进度细线移到头部行底缘；空态时头部行退化为纯 Agent 头。popover/dock/overlay/inline 形态及清单单例规则不变。

---

## 1. 组件概述

该组件是 Agent 运行过程中的**任务进度面板**，核心信息：

| 区域 | 内容 |
|---|---|
| 头部 | 面板标题（可折叠）、本次运行的耗时与 token 消耗、任务进度（已完成/总数） |
| 任务行 | 每行一个任务：状态图标（完成勾 / 拖拽把手）+ 自然语言任务描述（内嵌工具 chip）+ 工具参数 |

特征设计：**任务描述是自然语言句子，工具名以"chip（胶囊）"形式内嵌在句子中**，chip 内带工具品牌图标；参数以弱化文本跟在 chip 之后。

---

## 2. 视觉结构拆解

```
┌────────────────────────────────────────────────────────────────┐
│ ⌄  Tasks                        ┌───────────────┐              │
│                                 │ 4s · 637 tokens │  1/3       │  ← Header
│                                 └───────────────┘              │
│ ┌────┐                                                          │
│ │ ✅ │  I̶n̶s̶t̶a̶l̶l̶  ╭──────────╮  a̶p̶p̶                              │  ← 完成态：蓝勾 + 删除线
│ └────┘            │⚙ Slack │   （chip 中性色，整行删除线）          │
│                   ╰──────────╯                                  │
│ ┌────┐  ╭──────────────────╮                                    │
│ │ ⋮⋮ │  │ ⚙ Create channel  │  design-team                     │  ← 待执行态：拖拽把手
│ └────┘  ╰──────────────────╯   （chip 品牌色描边 + 参数文本）      │
│ ┌────┐  ╭──────────────────────╮                                │
│ │ ⋮⋮ │  │ ⚙ Invite users to cha… │  Pat Wasik                   │  ← 文本截断
│ └────┘  ╰──────────────────────╯                                │
└────────────────────────────────────────────────────────────────┘
```

### 层次结构

```
TaskPanel (卡片容器)
├── PanelHeader
│   ├── CollapseToggle (chevron 图标)
│   ├── Title ("Tasks")
│   ├── MetricsPill ("{duration} · {tokens} tokens")
│   └── ProgressText ("{done}/{total}")
└── TaskList
    └── TaskRow × N
        ├── StatusSlot          ← 二选一：CompletedCheck | DragHandle
        └── TaskContent
            ├── TextSegment     ← 普通文本片段
            ├── ToolChip        ← 工具 chip（icon + 工具名）
            ├── TextSegment
            └── ParamText       ← 参数（弱化色）
```

### 密度结构（任务数 > 4 时）

列表不再逐行平铺，改为**三段式渲染**——已完成的沉底、未轮到的不吵，只在焦点窗口内逐行展示：

```
┌──────────────────────────────────────────────────────────────┐
│ ⌄  Tasks                     16m 35s · 0 tokens        3/8   │  Header
│ ✓  3 项已完成                                            ⌄   │  段 A 汇总行（默认收起）
│ ◐  分节呈现设计并获取确认  ⚑                                 │  ← running：spinner + 文本提亮
│ ○  撰写设计文档到 docs/… 并提交                              │  ← pending：空心环，文本弱化
│ ○  规格自审                                                  │
│ ⌄  还有 2 项待执行                                            │  段 C 汇总行（默认收起）
└──────────────────────────────────────────────────────────────┘
```

- **段 A（已完成汇总）**：所有 completed 行收成一行 `✓ N 项已完成`（N=0 时不渲染）；点击展开查看明细。
- **焦点窗口**：running / failed 行**始终逐行完整显示**，不参与任何折叠；其后的 pending 只显示前 2 条。
- **段 C（溢出汇总）**：其余 pending 收成一行 `还有 N 项待执行`（N=0 时不渲染）；点击展开。
- 段 A/C 展开后，列表容器 `max-height: min(248px, calc(40vh - 56px))` 内部滚动，面板不再继续长高（完整规则见 §9.1）。

---

## 3. 设计规格（Design Tokens）

> 以下数值从截图像素采样与测量得出，深色主题。整体风格：低对比、圆角大、无锐利边框，靠"辉光 + 胶囊"塑造层次。

### 3.1 颜色

| Token | 值 | 用途 |
|---|---|---|
| `--panel-bg` | `#16161A` | 卡片主背景 |
| `--panel-glow` | `radial-gradient(ellipse at right, rgba(48,86,75,.55), transparent 60%)` | 卡片右侧绿色辉光（装饰性，叠加在背景上） |
| `--text-primary` | `#E6E6E9` | 标题、chip 内文字 |
| `--text-secondary` | `#9A9AA0` | 参数文本、chevron |
| `--text-completed` | `#6E6E74` | 完成态文字（配合删除线） |
| `--metrics-pill-bg` | `rgba(255,255,255,.06)` | 耗时/token 胶囊底 |
| `--metrics-pill-text` | `#B9B9BF` | 胶囊文字 |
| `--chip-bg` | `#1E1E22` | 完成态 chip 底（中性） |
| `--chip-border` | `#2A2A2E` | 完成态 chip 描边 |
| `--chip-accent-bg` | `rgba(76,32,44,.35)` | 待执行 chip 底（品牌色调） |
| `--chip-accent-border` | `#4C202C` | 待执行 chip 描边（酒红，来源近似工具品牌色，如 Slack aubergine 系） |
| `--check-blue` | `#136FF4` | 完成勾选圆 |
| `--check-mark` | `#FFFFFF` | 勾形 |
| `--grip-color` | `#55555B` | 拖拽把手圆点 |
| `--warning` | 暗 `#E7A13D` / 亮 `#B87A1E` | 优先级徽章（仅 HIGH，见 §3.4） |

> 说明：chip 的"酒红"强调色建议做成**按工具品牌色驱动的动态值**（`tool.brandColor` → 生成 `bg = color-mix(in srgb, brand 25%, transparent)`、`border = color-mix(in srgb, brand 55%, transparent)`），无品牌色时退化为中性色。完成态 chip 一律退为中性。

### 3.2 尺寸与间距

| 项 | 值 |
|---|---|
| 卡片圆角 | `24px` |
| 卡片内边距 | `24px 28px` |
| Header 高度 | `40px`，与列表间距 `16px` |
| chevron 图标 | `16px`，距标题 `12px` |
| MetricsPill | 高 `36px`，圆角 `999px`，水平 padding `16px`，字号 `15px` |
| ProgressText | 字号 `15px`，距 pill `14px` |
| 任务行高 | `50px`，行间距 `~2px` |
| 状态图标（蓝勾圆） | `24px` 圆形 |
| 拖拽把手 | `12px × 18px`，2 列 × 3 行圆点，点径 `2.5px`，点距 `4px` |
| 状态列与内容间距 | `16px` |
| ToolChip | 高 `38px`，圆角 `999px`，padding `4px 14px 4px 5px`，描边 `1px` |
| chip 内图标 | `26px` 圆形底（`#101013`）+ 内嵌 `16px` 品牌 logo |
| chip 与参数文本间距 | `12px` |
| 正文字号 | `16px` / 行高 `1.4` |
| 汇总行（段 A/C） | 高 `32px`，字号 `12px`，色 `--text-4`；hover `--hover` 底（圆角 8px）；段 A 左置 16px 完成勾图标、右置 9px chevron；段 C 左置 chevron |
| 密度模式任务行 | 高 `36px`（原生 50px 规格的紧凑化；MAFW 适配直接采用 36px）；状态图标 14–16px（§3.5），与文本间距 10px |
| 列表容器 | `max-height: min(248px, calc(40vh - 56px))`，超出内部滚动（约 6 行可见）；滚动条沿用全局细滚动条 |
| 面板总高上限 | Header（44px）+ 列表区，**任何任务量下 ≤ 40vh**，超出时继续压缩列表区而非撑高面板 |

### 3.3 字体

- 字体族：系统 UI 栈（`Inter, -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`）
- 标题 "Tasks"：`16px / 500`
- 任务文本：`16px / 400`
- 指标 pill、`1/3`：`15px / 400`，数字建议 `font-variant-numeric: tabular-nums`（跳秒时不抖动）

### 3.4 优先级标记（可选，priority flag）

任务可携带优先级标记，**仅 `high` 渲染**——medium/low 一律不渲染，避免每行都挂标记、把密度模型省出来的视觉噪音又还回去。

> v2.1：从大写 pill 改为**小旗帜图标**（Linear / Todoist 惯例）。pill 底+文字的面积感太重，多行并排时一片琥珀色斑块；旗帜只占 11px，色到意到。

| 项 | 值 |
|---|---|
| 图形 | 旗帜图标（Lucide `flag`），`11px`，stroke 1.8 |
| 颜色 | `--warning`；无底、无 pill 背景、无文字 |
| 位置 | 紧跟任务文本之后，间距 `6px`，`flex-shrink: 0` |
| completed 行 | 随整行一并降透明度（`opacity: .6`） |
| 可访问性 | 图标带 `title="高优先级"`；屏幕阅读器文本 `High priority` |

### 3.5 状态表达（图标制，无文字 pill）

> v2.1：移除 running/pending 文字 chip。业界共识（Claude Code、Copilot、Devin）是**状态全部由行首 StatusSlot 图标表达**，当前行动画、其余安静；文字 pill 是最占横向空间也最吵的表达，弃用。

| 状态 | StatusSlot（14–16px） | 行文本 |
|---|---|---|
| `pending` | **空心圆环**：13px 圆 + 1.5px 描边 `--text-5` | `--text-3`（弱化，未轮到的不抢戏） |
| `running` | **旋转 spinner**：14px 圆弧（1/4 弧长，`--accent`，stroke 1.5 round cap）+ 浅轨（`--text-5` 40%），`rotate 0.9s linear infinite` | `--text-1`（提亮，全场唯一动效） |
| `completed` | 16px 实心圆（`--accent`）+ 白色勾 | `--text-4` + 删除线 |
| `failed` | 16px 实心圆（`--danger`）+ × | 正常色 + 错误红参数 |
| 拖拽把手 | **非常显**：`sortable` 开启时，pending 行 hover 才在 StatusSlot 原位替换状态图标（6 点 grip）；松手即隐 | — |

区分 running 与 pending 不再依赖右侧 chip，而是「动画 + 文本明度」双重编码：spinner 是全场唯一在动的东西，一眼锁定。

---

## 4. 状态机

### 4.1 任务行状态

```
pending ──▶ running ──▶ completed
                 └────▶ failed（可选扩展）
```

| 状态 | 状态图标（v2.1，详见 §3.5） | 文本 | chip |
|---|---|---|---|
| `completed` | 实心圆 + 勾（原生截图语境为蓝 `#136FF4`；MAFW 主题本地化为 `--accent` 绿） | **整行删除线**，颜色 `--text-completed` | 中性底/描边，删除线穿透 |
| `running` | 旋转 spinner（`--accent` 圆弧，0.9s 线性） | `--text-1` 提亮 | 品牌色强调底 + 描边 |
| `pending` | 空心圆环（1.5px `--text-5` 描边） | `--text-3` 弱化 | 品牌色强调底 + 描边 |
| `failed`（扩展） | 红色圆（`--danger`）+ × | 正常色 + 错误红参数 | 红色描边 |
| 拖拽把手 | **非常显**：`sortable` 开启时 pending 行 hover 才替换状态图标出现（§4.3） | — | — |

> 截图中可见：行 1 为 completed（蓝勾 + 删除线 + 中性 chip），行 2/3 为 pending/running（把手 + 酒红 chip + 灰色参数）。
> v2.1 注：原生截图的"常显把手"是旧式表达；MAFW 适配按 §3.5 图标制执行——把手 hover 才出现，状态一眼可辨靠 spinner 动画而非文字。

### 4.2 面板状态

- **展开 / 折叠**：chevron 指示，折叠时仅保留 Header（标题 + 指标 + 进度）。
- **进度分页**：`1/3` 表示第 1 组 / 共 3 组任务（或当前聚焦第 1 项 / 共 3 项）。两种语义二选一，实现时与产品确认：
  - A. **分页**：列表每页 N 条，`current/totalPages`，配合点击或滚轮翻页；
  - B. **完成度**：`已完成数/总数`（推荐，Agent 场景更直觉）。截图语义更接近"1 of 3 步进行中"。
- **密度状态（任务数 > 4 时）**：列表按 §9.1 窗口规则分段渲染；段 A（已完成）、段 C（待执行溢出）各自有收起/展开两个状态，默认双收起。段的展开状态是**组件内 UI 状态**（不进 props）：面板整体折叠/展开时保留，新 run 开始时重置为双收起。
- **全部完成**：计时冻结在终值；`autoCollapseDelay`（默认 5s）后面板自动折叠为 Header 单行，进度文本变为 `✓ N/N`（accent 色），点击 Header 可重新展开查看全部。

### 4.3 把手 vs 状态图标

截图中 pending 行显示的是**拖拽把手**，暗示任务可人工重排。v2.1 起三者的槽位规则：

- `sortable = false`（默认）：StatusSlot 永远渲染状态图标（§3.5），把手不出现；
- `sortable = true`：pending 行 **hover 时**把手在原位替换状态图标（fade 100ms），松手/移出即恢复；completed/running/failed 行锁定不替换；
- 把手不再是状态的表达手段——pending 的"待执行"语义由空心环承担，把手只剩"我可拖"的 affordance。

### 4.4 摆放形态（placement）

| 形态 | 容器 | 列表高度 | 适用 |
|---|---|---|---|
| `bar`（MAFW 默认，v2.4） | **任务指示区**：嵌入宿主聊天头部行（与 Agent title 同一行，竖分隔线后 `flex-1`，随头部行固定不滚动），只渲染当前焦点行——spinner/静态图标 + 当前任务文本 + 旗帜 + `n/N` + 迷你进度轨 + 耗时；完整清单以锚定 popover 呈现（头部行下方，宽 `min(560px, 100%-32px)`，含 Header：指标 pill + 📌 + ×） | popover 内 `max-height: 320px` 内部滚动 | 所有视口（默认形态） |
| `dock`（bar 的 Pin 态） | 右侧 Dock：宽 320px，平铺 `--bg-base`，左缘 1px 分隔线，无辉光/阴影/圆角卡片；Header 高 44px + 收起按钮（= 拔回 bar） | `flex-1` 全高内部滚动 | 用户从 popover 📌 钉住；视口 ≥ 1200px |
| `overlay` | dock 的窄屏变体：`absolute` 右侧全高浮层，`--shadow-float`，点击外部/Esc 关闭 | 同 dock | 视口 < 1200px |
| `inline`（兜底/其它宿主） | 输入区上方浮层卡片（辉光 + 圆角 + `--shadow-float`） | `max-height: min(248px, calc(40vh - 56px))` 内部滚动 | 不具备侧栏条件的宿主环境 |

- **清单单例**：完整任务列表（§9.1 三段式）是同一份组件，按形态渲染在 popover 容器或 dock 容器里——SolidJS 就是同一个 `<TaskList>` 按 `placement` 条件挂载；vanilla JS 用 `appendChild` 迁移 DOM 节点（原型即如此）。行规格、密度模型、交互在所有形态下一致。
- bar 形态的空态：**无任务时指示区不渲染**，宿主头部行退化为纯 Agent 头；dock/overlay 形态下全部完成后不再自动折叠（没有让位压力），仅计时冻结、进度变 `✓ N/N`；inline 形态维持 5s 自动折叠（§4.2）。
- dock 形态行宽收窄兜底：chip `max-width` 220px → **160px**；空间不足时参数文本整体隐藏（优先级：chip > 首段文本 > 参数）。
- 摆放是宿主布局职责，组件只通过 `placement` 接收形态并切换上述容器/高度规则；📌 Pin、快捷键（`Ctrl/Cmd+J`）、会话记忆（`localStorage('mafw-tasks-placement')`）由宿主（MAFW Shell）实现，见《MAFW界面美化-开发文档》§4.6。

---

## 5. 交互行为

| 交互 | 行为 |
|---|---|
| 点击 Header / chevron | 折叠/展开列表；chevron 旋转 180°（`transform 200ms ease`），列表高度用 grid-rows 动画过渡 |
| MetricsPill | 运行中每秒刷新耗时；token 数随流式事件累加；数值滚动用淡入淡出过渡 |
| 进度 `1/3` | 随任务完成实时更新；若为分页语义，点击切换页 |
| 任务行 hover | 行背景 `rgba(255,255,255,.03)`；`sortable` 开启时 pending 行的把手替换状态图标淡入（100ms） |
| 拖拽排序 | 按住把手拖动调整 pending 任务顺序（仅 pending 可拖，completed/running 锁定）；松开后触发 `onReorder` |
| 点击 ToolChip | 打开该工具调用的详情（参数、返回结果、耗时）——建议作为 drawer/popover |
| 文本溢出 | chip 内文本 `max-width: 220px` 截断为 `…`；整行 `nowrap` + 尾部渐隐；hover 显示完整 tooltip |
| 任务完成瞬间 | 文本删除线动画（`::after` 宽度 0→100%，300ms）+ 蓝勾缩放弹入（scale 0.5→1，spring） |
| 新任务插入 | 行从下方滑入（translateY 8px + fade，200ms） |
| 段汇总行点击（`✓ N 项已完成` / `还有 N 项待执行`） | 展开/收起对应段（grid-rows 200ms）；chevron 旋转 90°；展开后若列表超高则容器内部滚动，running/failed 行保持 `scrollIntoView({block:'nearest'})` |
| 任务跨段迁移（pending→running→completed） | 行在三段之间移动时用 200ms fade 过渡，不硬跳；completed 增加时段 A 汇总行计数 +1 并短暂高亮（`--hover` 底 400ms） |
| 面板自动折叠（全部完成 5s 后） | grid-rows 收起到 0fr；倒计时期间用户点击/悬停面板可取消本次自动折叠 |

---

## 6. 数据结构

```ts
/** 工具品牌信息（chip 渲染用） */
interface ToolBrand {
  id: string;            // 'slack' | 'github' | ...
  name: string;          // 'Slack'
  iconUrl: string;       // 品牌 logo
  brandColor?: string;   // 十六进制，驱动 chip 强调色
}

/** 任务描述 = 有序片段数组，文本与工具引用交错 */
type TaskSegment =
  | { type: 'text'; content: string }        // 'Install ' / ' app'
  | { type: 'tool'; tool: ToolBrand; label: string };  // chip: 'Slack' / 'Create channel'

interface TaskItem {
  id: string;
  status: 'pending' | 'running' | 'completed' | 'failed';
  segments: TaskSegment[];   // 行内富文本：'Install [Slack] app'
  param?: string;            // chip 之后的参数：'design-team' / 'Pat Wasik'
  startedAt?: number;
  finishedAt?: number;
  error?: string;
  priority?: 'high' | 'medium' | 'low';   // 仅 high 渲染徽章（§3.4）
}

interface TaskPanelData {
  title: string;             // 'Tasks'
  collapsed: boolean;
  elapsedMs: number;         // 4s
  tokens: number;            // 637
  cursor: number;            // 当前第几步（1-based）
  tasks: TaskItem[];
}
```

> 密度分段的展开状态（段 A/C）**不进 `TaskPanelData`**——它是纯 UI 状态（组件内 `completedExpanded` / `overflowExpanded` 两个 signal），与会话数据无关；面板整体折叠时保留，新 run 重置。

对应截图的示例数据：

```ts
const demo: TaskPanelData = {
  title: 'Tasks', collapsed: false,
  elapsedMs: 4000, tokens: 637, cursor: 1,
  tasks: [
    { id: 't1', status: 'completed', segments: [
        { type: 'text', content: 'Install ' },
        { type: 'tool', tool: slack, label: 'Slack' },
        { type: 'text', content: ' app' } ] },
    { id: 't2', status: 'pending', param: 'design-team', segments: [
        { type: 'tool', tool: slack, label: 'Create channel' } ] },
    { id: 't3', status: 'pending', param: 'Pat Wasik', segments: [
        { type: 'tool', tool: slack, label: 'Invite users to channel' } ] },
  ],
};
```

---

## 7. 组件 API

```ts
interface TaskPanelProps {
  data: TaskPanelData;
  sortable?: boolean;                    // 是否允许拖拽排序，默认 false
  onToggleCollapse?: (collapsed: boolean) => void;
  onTaskClick?: (task: TaskItem) => void;
  onChipClick?: (task: TaskItem, tool: ToolBrand) => void;
  onReorder?: (orderedIds: string[]) => void;
  formatDuration?: (ms: number) => string;   // 默认 '4s' / '1m 23s'
  formatTokens?: (n: number) => string;      // 默认 '637 tokens' / '1.2k tokens'
  windowSize?: number;          // 焦点窗口内可见的 pending 条数：有 running 时默认 2，无 running 时自动 +1
  autoCollapseDelay?: number;   // 全部完成后自动折叠为 Header 单行的延迟，默认 5000ms；0 = 不自动折叠（dock 形态忽略此项，§4.4）
  placement?: 'bar' | 'dock' | 'overlay' | 'inline';   // 摆放形态，默认 'bar'（§4.4）；影响容器样式与列表高度规则
}
```

事件约定：

- `onReorder` 只携带 `pending` 任务的新顺序；completed/running 位置锁定。
- `onChipClick` 与 `onTaskClick` 互斥：点 chip 只触发 chip 事件（`stopPropagation`）。

---

## 8. 实现参考（SolidJS + Tailwind）

> 关键实现点只有三个：**片段渲染、chip 动态品牌色、完成态删除线动画**。以下为核心骨架。

```tsx
import { For, Match, Show, Switch, createMemo } from 'solid-js';

function ToolChip(props: { tool: ToolBrand; label: string; completed: boolean }) {
  const style = createMemo(() =>
    props.completed || !props.tool.brandColor
      ? { background: '#1E1E22', 'border-color': '#2A2A2E' }
      : {
          background: `color-mix(in srgb, ${props.tool.brandColor} 22%, transparent)`,
          'border-color': `color-mix(in srgb, ${props.tool.brandColor} 55%, transparent)`,
        }
  );
  return (
    <span
      class="inline-flex h-[38px] max-w-[220px] items-center gap-2 rounded-full border pl-[5px] pr-[14px] align-middle"
      style={style()}
    >
      <span class="flex h-[26px] w-[26px] shrink-0 items-center justify-center rounded-full bg-[#101013]">
        <img src={props.tool.iconUrl} alt="" class="h-4 w-4" />
      </span>
      <span class="truncate text-[16px] text-[#E6E6E9]">{props.label}</span>
    </span>
  );
}

/** v2.1 StatusSlot：图标制（§3.5）；把手仅在 sortable 且 hover 时替换（略） */
function StatusSlot(props: { status: TaskItem['status'] }) {
  return (
    <Switch>
      <Match when={props.status === 'completed'}>
        <span class="flex h-4 w-4 items-center justify-center rounded-full bg-[var(--accent)] text-[var(--on-accent)]">
          <CheckIcon class="h-2.5 w-2.5" />
        </span>
      </Match>
      <Match when={props.status === 'running'}>
        <svg class="h-3.5 w-3.5 animate-[spin_.9s_linear_infinite]" viewBox="0 0 14 14" fill="none">
          <circle cx="7" cy="7" r="5.5" stroke="var(--text-5)" stroke-opacity=".4" stroke-width="1.5" />
          <path d="M12.5 7A5.5 5.5 0 0 0 7 1.5" stroke="var(--accent)" stroke-width="1.5" stroke-linecap="round" />
        </svg>
      </Match>
      <Match when={props.status === 'failed'}>
        <span class="flex h-4 w-4 items-center justify-center rounded-full bg-[var(--danger)] text-white">
          <XIcon class="h-2.5 w-2.5" />
        </span>
      </Match>
      <Match when={true}>{/* pending：空心圆环 */}
        <span class="h-[13px] w-[13px] rounded-full border-[1.5px] border-[var(--text-5)]" />
      </Match>
    </Switch>
  );
}

function TaskRow(props: { task: TaskItem }) {
  const done = () => props.task.status === 'completed';
  return (
    <div class="flex h-9 items-center gap-2.5 rounded-lg px-1 hover:bg-white/[.03]">
      <StatusSlot status={props.task.status} />

      {/* 片段内容 */}
      <p
        class="flex min-w-0 items-center gap-1.5 text-[16px]"
        classList={{ 'text-[#6E6E74] line-through decoration-[#6E6E74]/70': done() }}
      >
        <For each={props.task.segments}>
          {(seg) =>
            seg.type === 'text'
              ? <span class="whitespace-pre">{seg.content}</span>
              : <ToolChip tool={seg.tool} label={seg.label} completed={done()} />
          }
        </For>
        <Show when={props.task.param}>
          <span class="ml-1.5 truncate text-[#9A9AA0] no-underline">{props.task.param}</span>
        </Show>
      </p>

      {/* priority flag：文本之后（仅 high，§3.4） */}
      <Show when={props.task.priority === 'high'}>
        <FlagIcon class="h-[11px] w-[11px] shrink-0 text-[var(--warning)]" />
      </Show>
    </div>
  );
}
```

卡片容器与辉光：

```tsx
<section class="relative overflow-hidden rounded-3xl bg-[#16161A] px-7 py-6">
  {/* 右侧绿色辉光（装饰层，不挡交互） */}
  <div class="pointer-events-none absolute inset-0
              bg-[radial-gradient(ellipse_at_right,rgba(48,86,75,.55),transparent_60%)]" />
  <header class="relative flex h-10 items-center gap-3">
    <ChevronDown class="h-4 w-4 text-[#9A9AA0] transition-transform"
                 classList={{ '-rotate-90': props.data.collapsed }} />
    <h2 class="text-[16px] font-medium text-[#E6E6E9]">{props.data.title}</h2>
    <div class="ml-auto flex items-center gap-3.5">
      <span class="rounded-full bg-white/[.06] px-4 py-1.5 text-[15px] tabular-nums text-[#B9B9BF]">
        {formatDuration(props.data.elapsedMs)} · {formatTokens(props.data.tokens)}
      </span>
      <span class="text-[15px] tabular-nums text-[#E6E6E9]">
        {props.data.cursor}/{props.data.tasks.length}
      </span>
    </div>
  </header>
  {/* 折叠动画：grid-rows 技巧 */}
  <div class="grid transition-[grid-template-rows] duration-200"
       classList={{ 'grid-rows-[0fr]': props.data.collapsed, 'grid-rows-[1fr]': !props.data.collapsed }}>
    <div class="overflow-hidden">
      <For each={props.data.tasks}>{(t) => <TaskRow task={t} />}</For>
    </div>
  </div>
</section>
```

密度分段渲染（§9.1 的实现核心是一个派生 memo，模板按三段输出）：

```tsx
/** 把 tasks 派生成 段A / 焦点窗口 / 段C；总数 ≤ 4 时分段不启用 */
function useSegments(tasks: () => TaskItem[], windowSize = 2) {
  return createMemo(() => {
    const all = tasks();
    if (all.length <= 4) return { dense: false, done: [], focus: all, more: [] };
    const done   = all.filter(t => t.status === 'completed');
    const active = all.filter(t => t.status !== 'completed');   // 保持原始顺序
    const hasRun = active.some(t => t.status === 'running');
    const k      = hasRun ? windowSize : windowSize + 1;        // 无 running 时多露一条
    const focus: TaskItem[] = [], more: TaskItem[] = [];
    let shown = 0;
    for (const t of active) {
      // running / failed 恒显且不计入窗口；pending 只露前 k 条
      if (t.status !== 'pending' || shown < k) {
        focus.push(t);
        if (t.status === 'pending') shown++;
      } else more.push(t);
    }
    return { dense: true, done, focus, more };
  });
}

// 组件内 UI 状态（新 run 重置）
const [doneExpanded, setDoneExpanded] = createSignal(false);
const [moreExpanded, setMoreExpanded] = createSignal(false);
```

```
TaskList (滚动容器 max-height: min(248px, calc(40vh - 56px)); overflow-y: auto)
├── [dense] 段A 汇总行  "✓ {done.length} 项已完成"   (done.length > 0 才渲染)
├── [dense] CollapseRegion(doneExpanded) → TaskRow × done
├── TaskRow × focus                        ← running/failed 恒显 + 前 k 条 pending
├── [dense] 段C 汇总行  "还有 {more.length} 项待执行"  (more.length > 0 才渲染)
└── [dense] CollapseRegion(moreExpanded) → TaskRow × more
```

> React 版本差异仅在响应式写法（`useMemo`/className 拼接），结构与样式完全一致。拖拽排序推荐 `dnd-kit`（React）或 `@thisbeyond/solid-dnd`（SolidJS），只把 `DragHandle` 作为 drag activator。

---

## 9. 边界与异常

| 场景 | 处理 |
|---|---|
| 无任务 | Header 保留，列表区显示 `No tasks yet`（弱化色居中），进度显示 `0/0` |
| 单行过长 | chip `max-width` 截断；行尾参数允许隐藏（优先级：保留 chip > 保留首段文本 > 保留参数） |
| 任务数 > 4 | **启用密度模型（§9.1）**：三段式渲染，不是简单加滚动——默认可见 = 完成汇总行 + running/failed + ≤2 条 pending + 溢出汇总行 |
| 任务数 > 一屏（段展开后） | 列表容器内部滚动，不撑高容器：inline 形态 `max-height: min(248px, calc(40vh - 56px))`；dock/overlay 形态 `flex-1` 全高（§4.4）；running/failed 行自动 `scrollIntoView({block:'nearest'})` |
| 全部完成 | 计时冻结在终值；`autoCollapseDelay`（默认 5s）后自动折叠为 Header 单行，进度变 `✓ N/N`（accent 色） |
| failed 存在 | failed 行与 running 同权，**永不折叠**进任何汇总段，也不计入 pending 窗口配额 |
| 耗时 > 1h | 格式降级：`59s` → `3m 12s` → `1h 5m` |
| token 大数 | `>= 10000` 显示 `12.4k tokens` |
| 流式更新频率 | 指标 pill 更新做 200ms 节流，避免每 token 重渲染 |
| 无品牌图标 | chip 图标位显示首字母圆形占位 |

### 9.1 密度模型（窗口规则）

任务数 > 4 时启用。目标：**默认可见内容恰好是"正在做什么 + 接下来做什么"**——已完成的不再逐行占屏，没轮到的先不吵；inline 形态下面板总高 ≤ 40vh，dock 形态下利用全高但仍按段折叠降噪。

1. **段 A**：所有 `completed` → 列表顶部汇总行 `✓ N 项已完成`（N=0 不渲染；默认收起，点击展开明细）。
2. **焦点窗口**：`running` / `failed` 行始终逐行显示，不参与任何折叠，也不占用 pending 窗口配额。
3. **pending 窗口**：只显示前 `windowSize` 条 pending——有 running 时默认 **2** 条，无 running（未开始 / 已中断）时 **3** 条。
4. **段 C**：其余 pending → 列表底部汇总行 `还有 N 项待执行`（N=0 不渲染；默认收起，点击展开）。
5. **展开兜底**：段 A/C 任一段展开后，列表容器内部滚动——inline 形态 `max-height: min(248px, calc(40vh - 56px))`，dock/overlay 形态 `flex-1` 全高（§4.4）；running/failed 行保持可见（`scrollIntoView({block:'nearest'})`）。
6. **状态归属**：段展开/收起是组件内部 UI 状态，不进 props；面板整体折叠时保留；新 run 重置为双收起。
7. **不启用条件**：任务总数 ≤ 4 时全部平铺，不出现任何汇总行——小列表不值得付出折叠的交互成本。

---

## 10. 验收清单

- [ ] 深色卡片 + 右侧绿色辉光，圆角 24px
- [ ] Header 三要素齐全：标题可折叠 / 指标 pill / 进度 `n/N`
- [ ] 完成态：勾圆（MAFW 用 `--accent` 绿）+ 整行删除线 + chip 退为中性
- [ ] 状态图标制（v2.1）：pending 空心环 / running spinner（全场唯一动效）/ failed 红 ×；无任何文字 pill
- [ ] 拖拽把手仅在 sortable 且 hover 时出现，非常显
- [ ] chip 内文本 220px 截断，hover tooltip
- [ ] 折叠动画、完成动画、插入动画流畅（≤300ms）
- [ ] 指标数字 tabular-nums，跳变不抖动
- [ ] pending 任务可拖拽排序并回调 `onReorder`（sortable 开启时）
- [ ] 任务数 > 4 时启用三段式密度：默认可见 = 完成汇总行 + running/failed + ≤2 条 pending + 溢出汇总行
- [ ] 段 A/C 可点击展开；展开后列表 248px 内部滚动，面板总高 ≤ 40vh，running 行保持可见
- [ ] running 行 spinner + 文本 `--text-1` 提亮，pending 行空心环 + `--text-3` 弱化（§3.5）；HIGH 为 11px 旗帜图标（§3.4），medium/low 不出现
- [ ] 全部完成：计时冻结，5s 后自动折叠为 Header 单行，进度变 `✓ N/N`
- [ ] failed 行永不进汇总段；任务跨段迁移有 200ms 过渡
