# PermissionCard 权限询问卡片组件 — 开发文档

> Agent 执行**有副作用的操作**（跑命令、写/删文件、访问网络、重启服务）前，在聊天流内请求的权限确认卡片。
> 与《询问卡片组件-开发文档》（AskCard）配对：**AskCard 管"决策"（怎么选），PermissionCard 管"放行"（能不能做）**。
> 色板复用 v3 tokens + `--warning`；新增 1 组危险色 `--danger-*`。

---

## 1. 组件概述

```
┌──────────────────────────────────────────────────────┐
│ 🛡 Manager 请求权限              ● 高风险  · 等待确认  │ ← Header：盾牌 + 风险徽章 + 状态
│──────────────────────────────────────────────────────│
│ 执行 Shell 命令                                        │ ← 操作类型 15px/500
│ ┌──────────────────────────────────────────────────┐ │
│ │ $ rm -rf /tmp/mafw-cache && pnpm build           │ │ ← 命令/参数 mono 块（可滚）
│ └──────────────────────────────────────────────────┘ │
│ 影响：删除缓存目录并触发全量构建，约 2 分钟，不可中断     │ ← 影响说明（可选）13px text-3
│                                                        │
│ Y 允许一次 · A 本会话始终允许 · N 拒绝                   │
│                  [ 拒绝 ]  [ 本会话始终允许 ]  [ 允许一次 ]│ ← Footer：三按钮
└──────────────────────────────────────────────────────┘
```

### 层次结构

```
PermissionCard
├── CardHeader     → 盾牌图标 + "{agent} 请求权限" + 风险徽章(高/中) + 状态徽章
├── ActionTitle    → 操作类型（"执行 Shell 命令" / "删除文件" / …）
├── PayloadBlock   → 命令/路径/参数的 mono 展示块
├── ImpactText?    → 影响说明（可选）
└── CardFooter     → 快捷键提示 + [拒绝] [本会话始终允许] [允许一次]
```

与 AskCard 的关键差异：**不用选项列表，用三按钮**——权限是"放行粒度"决策（一次 / 始终 / 拒绝），不是开放式选择。

---

## 2. 设计规格

### 2.1 新增语义色

| Token | 暗 | 亮 | 用途 |
|---|---|---|---|
| `--danger` | `#E5484D` | `#C93A3E` | 高风险竖条/徽章、高风险时的"允许一次"按钮、拒绝态摘要 |
| `--danger-dim` | `rgba(229,72,77,.12)` | `rgba(201,58,62,.10)` | 高风险徽章底、危险按钮 hover 底 |
| `--danger-text` | `#F0757A` | `#C93A3E` | 暗色下的危险文字 |

中风险沿用 `--warning`（暗 `#E7A13D` / 亮 `#B87A1E`）。

### 2.2 容器与 Header

| 元素 | 规格 |
|---|---|
| 容器 | `--bg-overlay` + 1px `--border-subtle` + 圆角 16px；**左侧 3px 竖条** = 高风险 `--danger` / 中风险 `--warning`；宽 100%（≤760px） |
| Header | 高 40px，padding `0 16px`，下缘 1px `--border-subtle` |
| 盾牌图标 | 16px，`--danger` 或 `--warning` 色 |
| 标题 | `{agentName} 请求权限` 13px/500 `--text-2` |
| 风险徽章 | pill 高 20px 11px：高风险 = `--danger-dim` 底 + `--danger-text`；中风险 = warning 12% 底 + `--warning` |
| 状态徽章 | "等待确认"（脉动点，色随风险级）；已允许 = `--accent-dim`/`--accent-text`；已拒绝 = `--danger-dim`/`--danger-text` |

### 2.3 操作区

| 元素 | 规格 |
|---|---|
| 操作类型 | 15px/500 `--text-1`，padding `14px 16px 8px`；取值枚举：`执行 Shell 命令` / `写入文件` / `删除文件` / `访问网络` / `重启服务` / `安装依赖` / 自定义 |
| PayloadBlock | margin `0 16px`，`--bg-float` + 1px `--border-subtle` 圆角 8px，padding `10px 12px`；内容 13px mono `--text-2`（命令主体）+ 路径/高危片段可染 `--danger-text`；`max-height: 96px` 内部滚动；hover 右上角出现复制按钮 |
| 影响说明 | 13px `--text-3`，padding `8px 16px 0`；写清**范围 + 时长 + 可逆性**（"约 2 分钟，不可中断"），不超过 2 行 |

### 2.4 Footer 三按钮（从右到左 = 主到次）

| 按钮 | 规格 |
|---|---|
| 允许一次（主） | 高 32px padding `0 16px` 圆角 8px 13px/500。**高风险 = `--danger` 底 + 白字**（用按钮颜色传递严重性）；中风险 = `--accent` 底 + `--on-accent`。快捷键 `Y` 或 `Enter` |
| 本会话始终允许（次） | 高 32px padding `0 14px` 圆角 8px，`--bg-float` 底 + 1px `--border-subtle` + `--text-2`；hover `--hover-strong`。快捷键 `A` |
| 拒绝（次） | ghost：13px `--text-3`，hover `--danger-dim` 底 + `--danger-text`。快捷键 `N` 或 `Esc` |
| 快捷键提示 | 左侧 11px `--text-5`：`Y 允许一次 · A 始终允许 · N 拒绝` |

> "本会话始终允许"语义：把该操作类型（+ 同 payload 模式）写入**会话级权限规则**，本次会话内同类操作自动放行；规则随会话结束失效，且可在 Approvals 页查看/撤销。

---

## 3. 状态机

```
pending ──允许一次────▶ allowed-once
   │──始终允许────▶ allowed-always（写入会话规则）
   │──拒绝────────▶ denied（agent 收到拒绝原因/可附言）
   │──会话中断────▶ expired（视为拒绝）
```

| 状态 | 摘要形态（单行 40px） |
|---|---|
| allowed-once | `--accent` ✓ + 操作类型 + payload truncate + "已允许"徽章 |
| allowed-always | 同上 + 徽章文案"始终允许" |
| denied / expired | `--danger` ✗ + 操作类型 + "已拒绝"徽章，整体 60% 透明 |

**拒绝附言**（可选增强）：点拒绝时按钮旁展开一行输入"告诉 agent 为什么（可选）"，Enter 随拒绝信号一起回传；为空则仅拒绝。默认开启——这是 agent 自我修正的关键信号。

---

## 4. 交互行为

| 交互 | 行为 |
|---|---|
| 键盘 | pending 且无输入焦点：`Y`/`Enter` 允许一次，`A` 始终允许，`N`/`Esc` 拒绝；输入焦点在拒绝附言框时仅 `Enter` 生效 |
| 高风险确认 | `risk=high` 时"允许一次"**首次点击变为确认态**（按钮文案变"确认执行？"，1.5s 内再点才生效）——防误触；中风险无此步骤 |
| 多张 pending | **串行队列**：同一会话同时只展示 1 张 pending 权限卡，后续请求排队（badge 显示"还有 N 项"）；权限是阻塞语义，不允许并行点一堆 |
| Approvals 联动 | pending 权限卡实时计入顶部导航 Approvals 计数徽章；在 Approvals 页操作后，聊天流内卡片同步终态 |
| 与 AskCard 并存 | 权限卡优先展示（阻塞级高于提问） |
| 自动放行命中 | "始终允许"规则命中的操作不再出卡，但在聊天流留一行 11px `--text-5` 痕迹：`已自动允许：pnpm build（会话规则 #2）`——可审计 |

---

## 5. 数据结构

```ts
type RiskLevel = 'medium' | 'high';
type ActionType = 'shell' | 'file-write' | 'file-delete' | 'network' | 'service' | 'install' | 'custom';

interface PermissionCardData {
  id: string;
  agentName: string;
  status: 'pending' | 'allowed-once' | 'allowed-always' | 'denied' | 'expired';
  risk: RiskLevel;
  action: {
    type: ActionType;
    title: string;            // '执行 Shell 命令'
    payload: string;          // 'rm -rf /tmp/mafw-cache && pnpm build'
    dangerousParts?: string[];// payload 中需染危险色的片段：['rm -rf']
  };
  impact?: string;            // '删除缓存目录并触发全量构建，约 2 分钟'
  denyNotePlaceholder?: string;
  createdAt: number;
}

interface SessionRule {       // "始终允许"产物
  id: string;
  actionType: ActionType;
  pattern: string;            // payload 匹配模式
  createdBy: string;          // cardId
}
```

## 6. 组件 API

```ts
interface PermissionCardProps {
  data: PermissionCardData;
  queueLength?: number;                       // 排队中的后续请求数
  onAllowOnce?: () => void;
  onAllowAlways?: (rule: Omit<SessionRule, 'id' | 'createdBy'>) => void;
  onDeny?: (note?: string) => void;
}
```

终态由父级回写 `status`；组件不持有权限判断逻辑（规则匹配在 gateway/host 侧）。

---

## 7. 边界与异常

| 场景 | 处理 |
|---|---|
| payload 超长/多行 | mono 块 96px 内部滚动；复制按钮始终可得完整内容 |
| 无影响说明 | 不渲染 ImpactText，卡片收短 |
| 高危片段标注 | `dangerousParts` 在 payload 中做子串染色；无则不染 |
| 拒绝附言为空 | 允许直接拒绝，附言非必填 |
| 队列中会话结束 | 全部 pending → expired，agent 收到统一中断信号 |
| 亮色主题 | `--danger` 已给亮色系值；红/琥珀竖条在白底上保持可辨 |
| 安全红线 | **删除/执行类高风险不允许"始终允许"覆盖到全模式**：`file-delete` 与含 `rm|del|format` 的 shell 的"始终允许"只锁定精确 payload，不生成通配规则 |

---

## 8. 验收清单

- [ ] 竖条/徽章/主按钮三色分级：高风险红、中风险琥珀
- [ ] payload mono 块完整展示命令，高危片段染色，可复制
- [ ] 三按钮主次明确：允许一次（实色）> 始终允许（描边）> 拒绝（ghost）
- [ ] 高风险"允许一次"需二次确认（文案变"确认执行？"）
- [ ] 键盘 Y / A / N(Esc) 全部可用
- [ ] 始终允许后写入会话规则，同类操作不再出卡且留自动放行痕迹
- [ ] 拒绝可附言并随信号回传
- [ ] 终态折叠单行摘要（已允许绿 / 已拒绝红 60% 透明）
- [ ] pending 计入 Approvals 徽章，跨页操作状态同步
- [ ] 同会话权限请求串行排队，显示"还有 N 项"
- [ ] 双主题下红/琥珀语义清晰可辨
