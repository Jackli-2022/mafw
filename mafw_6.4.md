# MAFW v6.4 架构文档 —— 认知深化架构（Cognitive Deepening Architecture）

> **版本**：v6.4 | **代号**：Deepen | **状态**：设计冻结，可进入开发
>
> **一句话定位**：在 v6.3"谐波记忆"的基础上，引入人类认知深化机制——**让记忆主动生长：变深、联网、结晶。**


## 一、v6.4 的设计动机

### 1.1 v6.3 的局限

v6.3 构建了一个完美的**静态记忆仓库**——存储高效、检索精准、跨层合并。但它仍然是被动的：

| 问题 | 表现 | 后果 |
| :--- | :--- | :--- |
| **遗忘曲线平坦** | 所有记忆以固定速率衰减（-0.01/天） | 重要经验与琐碎日志同等对待，踩坑教训可能被遗忘 |
| **记忆孤立** | 检索命中多条记忆，但系统不建立它们之间的关联 | 无法形成知识网络，Agent 看不到"这件事与那件事有关" |
| **知识不升华** | 重复的叙事不会自动提炼为原则 | 经历了很多次，但系统级智慧不增长 |
| **强化靠运气** | 记忆 Energy 只有在被检索到时才提升 | 重要但不常被查的记忆（如"年度大故障"）会缓慢饿死 |

### 1.2 人类认知的启示

人类大脑有四大机制维持知识活力：

1. **间隔复习（Spaced Repetition）**：复习间隔逐渐拉长，每次提取都强化记忆。
2. **联想网络（Associative Network）**：同时激活的神经元建立连接，形成知识网络。
3. **抽象蒸馏（Abstraction Distillation）**：多次相似经历→一个通用原则，细节褪去，原理浮现。
4. **显著度加权（Salience Weighting）**：高情绪/高后果的事件被牢牢记住，遗忘速度远慢于日常琐事。

**v6.4 将这四种机制工程化落地。**


## 二、v6.4 架构全景

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                      MAFW v6.4（认知深化架构）全景                                  │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌─────────────────────────── 访问层（MCP Tools + Hooks）───────────────────────┐  │
│  │  • mafw_search（强化联想预取）  • mafw_review（主动复习触发器）               │  │
│  │  • mafw_observe（感知显著度）   • 其他现有接口                               │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │              ★ 统一谐波索引 + 认知图（Harmonic Index + Cognitive Graph）       │  │
│  │  文件1：.mafw/memory/.harmonic_index.json（统一索引，新增深化字段）          │  │
│  │  文件2：.mafw/memory/.cognitive_graph.json（★新增：联想权重邻接表）          │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │          记忆核心层（五层存储——数据模型同步升级，增加深化元数据）              │  │
│  │                                                                              │  │
│  │  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────────────────────────┐│  │
│  │  │  T1   │  │  T2   │  │  T3   │  │  T4   │  │          L5               ││  │
│  │  │工作记忆│  │情景记忆│  │语义记忆│  │过程记忆│  │  全局元认知              ││  │
│  │  ├───────┤  ├───────┤  ├───────┤  ├───────┤  ├───────────────────────────┤│  │
│  │  │Level 0│  │Level 1│  │Level 2│  │Level 2│  │        Level 3             ││  │
│  │  │(原始) │  │(叙事) │  │(事实) │  │(流程) │  │        (公理)              ││  │
│  │  └───────┘  └───────┘  └───────┘  └───────┘  └───────────────────────────┘│  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │          ★ 新增：认知治理层（后台守护协程 - 空闲时运行）                      │  │
│  │                                                                              │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ 1. 间隔复习调度器（Review Scheduler）                               │    │  │
│  │  │    • 检查 Energy > 0.5 的记忆是否超过复习间隔（1→2→4→8天）         │    │  │
│  │  │    • 生成复习任务队列 → 触发 Agent 空闲时调用 mafw_review           │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ 2. 联想网络强化器（Association Builder）                            │    │  │
│  │  │    • 每当检索同时命中多条记忆 → 两两连接强度 +1                     │    │  │
│  │  │    • 周期性剪枝：移除强度 < 2 的弱连接，保持图稀疏                 │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ 3. 抽象蒸馏器（Abstraction Distiller）                              │    │  │
│  │  │    • 监测 T2 中相同主题 > 3 条 → 自动提炼为 1 条 T3（Level 2）      │    │  │
│  │  │    • 监测 T4 中成功执行 > 5 次 → 自动上升为 L5 启发式（Level 3）    │    │  │
│  │  │    • 蒸馏后，原始旧记忆降权（Energy 固定为 0.4）                   │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────┘
```


## 三、升级版数据模型：谐波单元 v2.0

为了支撑"认知深化"，每条记忆的元数据大幅增强：

```json
{
  "id": "mem_001",
  "goal_id": "goal_payment",
  "memory_type": "semantic",
  "primary_abstraction": "支付回调幂等键设计",
  "cue_anchors": ["幂等", "Redis", "支付"],
  "memory_value": "使用订单号+状态机作为幂等键...",

  // ★★★ v6.4 新增深化字段 ★★★
  "abstraction_level": 2,           // 0=原始(T1), 1=叙事(T2), 2=事实(T3/T4), 3=公理(L5)
  "salience": 1.5,                  // 显著度 (0.5~1.5)，报错/宕机=1.5，常规=0.5
  "review_count": 3,                // 已复习次数
  "last_reviewed": "2026-07-04",    // 上次复习日期
  "top_associations": ["mem_002", "mem_005"], // ★ 联想预取指针（Top 2 死党）

  "energy": 0.85,
  "created_at": "2026-06-20",
  "updated_at": "2026-07-04"
}
```

### 3.1 独立存储的联想邻接表（.cognitive_graph.json）

```json
{
  "version": 1,
  "updated_at": "2026-07-05T10:00:00Z",
  "edges": [
    {"source": "mem_001", "target": "mem_002", "weight": 12},
    {"source": "mem_001", "target": "mem_005", "weight": 8},
    {"source": "mem_002", "target": "mem_005", "weight": 5}
  ]
}
```


## 四、四大认知引擎的工作流详解

### 4.1 显著度感知（Salience Perceptor）

**触发时机**：T1 写入 `raw_observation` 时自动触发。

**实现逻辑**：

```python
def calculate_salience(raw_observation: str) -> float:
    # 正则扫描关键词
    high_salience_patterns = [
        r'(?i)error|crash|fail|丢失|数据损坏|宕机',
        r'(?i)critical|emergency|严重'
    ]
    low_salience_patterns = [
        r'(?i)info|success|完成|正常',
        r'(?i)debug|trace|verbose'
    ]
    
    if any(re.search(p, raw_observation) for p in high_salience_patterns):
        return 1.5
    elif any(re.search(p, raw_observation) for p in low_salience_patterns):
        return 0.5
    else:
        return 1.0
```

**影响**：**衰减公式重写**。
```
日衰减 = 0.01 × (1 / salience)
```
- `salience = 1.5`（重大故障）→ 日衰减 0.0067（常规的 1/3）
- `salience = 0.5`（常规日志）→ 日衰减 0.02（是常规的 2 倍）

**效果**：踩坑经验长期高亮，日常日志快速褪去。

### 4.2 间隔复习调度器（Review Scheduler）

**触发时机**：后台守护协程，空闲时运行。

**实现逻辑**：

```python
def get_next_review_interval(review_count: int) -> int:
    # 基础间隔 1 天，每次复习后翻倍
    # 1 → 2 → 4 → 8 → 16 → 32 天
    return 1 * (2 ** review_count)

def schedule_reviews():
    now = datetime.now()
    for entry in harmonic_index:
        if entry.energy < 0.5:
            continue
        days_since_last = (now - entry.last_reviewed).days
        next_interval = get_next_review_interval(entry.review_count)
        if days_since_last >= next_interval:
            queue_review_task(entry.id)
```

**收益**：模拟人类"提取练习"，每次成功复习，Energy 额外 +0.05。

### 4.3 联想网络强化器（Association Builder）

**写入连接**（检索时触发）：
```
当 mafw_search 返回 N 条结果时（N > 1）：
  对返回列表中的记忆两两建立连接：
    edges[(id_i, id_j)].weight += 1
  更新 top_associations：
    对每条记忆，取权重最高的 2 个邻居写入 top_associations
```

**读取预取**（检索时触发）：
```
当某条记忆被命中时：
  读取其 top_associations 指针
  提前加载关联记忆的 primary_abstraction
  随检索结果一起注入上下文（Agent 无需二次检索）
```

**剪枝**（每周运行）：
```
移除 weight < 2 的弱连接，保持图稀疏
```

**收益**：Agent 查"支付超时"时，自动附带"网关配置"和"重试策略"的摘要，实现触类旁通。

### 4.4 抽象蒸馏器（Abstraction Distiller）

**触发时机**：压缩器运行时检查 + 每周定时巡检。

**规则一：情景 → 语义（T2 → T3）**
```
检测：T2 中相同 primary_abstraction 词根的叙事 ≥ 3 条
动作：
  1. 合并生成 1 条 T3 事实（Level 2）
  2. 删除重复细节，只保留核心因果链
  3. 原 3 条 T2 的 Energy 锁定为 0.4（不再参与检索排序）
  4. 新 T3 的 merged_from 记录旧 T2 ID
```

**规则二：流程 → 公理（T4 → L5）**
```
检测：T4 中某流程被成功执行 ≥ 5 次（successRate > 0.9）
动作：
  1. 提取核心步骤，生成 1 条 L5 全局启发式（Level 3）
  2. 写入 l5/heuristics.json
  3. 原 T4 流程保留，但 abstraction_level 标记为 2
```

**收益**：细节褪去，原则浮现，记忆密度提高。


## 五、升级后的上下文注入

相比 v6.3，Agent 的上下文中多了 **"预取联想"** 和 **"复习提醒"** 两块内容：

```xml
<mafw_prior_knowledge>
【热记忆·高显著】
[事实|T3|能量88%|显著度1.5] 支付回调幂等键设计：使用订单号+状态机作为幂等键...
  → ★ 联想预取：同时加载 “网关超时配置 (关联强度92%)”、“重试策略 (关联强度78%)”

【待复习提醒】
[复习任务] 请简要回顾 “JWT并发刷新死锁” 的解决方案（上次复习：3天前）。
</mafw_prior_knowledge>
```


## 六、v6.3 vs v6.4 完整对比

| 维度 | v6.3（谐波记忆） | **v6.4（认知深化）** |
| :--- | :--- | :--- |
| **遗忘机制** | 固定线性衰减（-0.01/天） | **艾宾浩斯曲线** + 显著度保护（重要故障衰减慢 3 倍） |
| **记忆连接** | 仅靠共现锚点（静态） | **动态联想网络**，检索时自动预取"死党记忆" |
| **知识提炼** | 被动等待 Spiral 压缩 | **主动蒸馏**（3 条叙事→1 条事实，5 次流程→1 条公理） |
| **强化手段** | 被动命中才升 Energy | **间隔复习**，主动调度提取练习，复习次数越多越牢 |
| **上下文内容** | 仅当前命中结果 | 命中结果 + **联想预取** + **复习任务** |
| **存储新增** | 五层 + 索引 | 五层 + 索引 + **认知图（邻接表）** |
| **总文件大小** | ~2.5 MB / 项目 | ~3 MB / 项目（增加 < 0.5 MB 的邻接表） |
| **新增代码量** | — | ~500 行（四个引擎合计） |
| **LLM 依赖** | 完全可选（仅润色） | **仍完全可选**（所有引擎纯规则运行） |


## 七、实施路线图（按 ROI 排序）

| 优先级 | 任务 | 改动范围 | 收益 | 工期 |
| :--- | :--- | :--- | :--- | :--- |
| **P0** | 显著度感知 | 修改 T1 写入逻辑 + 衰减公式（1 处改动） | 高 | 0.5 天 |
| **P1** | 联想预取 | 检索逻辑增加读 `top_associations` + 注入上下文 | 高 | 1 天 |
| **P2** | 间隔复习调度器 | 新增后台协程 + `mafw_review` Tool | 中 | 2 天 |
| **P3** | 抽象蒸馏器 | 升级合并逻辑（T2→T3，T4→L5） | 中 | 3 天 |
| **P4** | 联想网络剪枝 | 定时清理弱连接 | 低 | 0.5 天 |

**建议顺序**：P0 → P1 → P2 → P3（P4 可并入 P1 随带实现）


## 八、从 v6.0 到 v6.4 的完整演进链

```
v6.0  愿景蓝图（Aligned Spiral）
   │
   │  "五层记忆 + 自适应螺旋" 首次提出
   ▼
v6.1  数据契约固化（Memory Spec）
   │  • 统一命名 T1-T4 + L5
   │  • L5 明确定义
   │  • T1 计数规则锁定
   ▼
v6.2  存查解耦第一步（Index Layer）
   │  • 引入 .memory_index.json
   │  • 层级降为存储分区
   ▼
v6.3  谐波记忆系统（Harmonic Memory）
   │  • 统一五层数据模型
   │  • 层级降级为标签
   │  • 跨层自动合并
   │  • 检索无视层级
   ▼
v6.4  认知深化架构（Cognitive Deepening）
   │  • 显著度感知（踩坑经验 3 倍慢遗忘）
   │  • 间隔复习（主动调度提取练习）
   │  • 联想网络（检索时预取"死党记忆"）
   │  • 抽象蒸馏（叙事→事实，流程→公理）
   ▼
🔒 设计冻结，进入开发
```


## 九、总结

> **v6.4 让 MAFW 从一个"被动的文件柜"进化为"主动生长的有机大脑"——记忆会变深、会联网、会结晶。**

| 维度 | v6.3 | v6.4 |
| :--- | :--- | :--- |
| **哲学** | 记忆是**存放**的文件 | 记忆是**生长**的神经网络 |
| **遗忘** | 线性、无差别 | **艾宾浩斯 + 显著度保护** |
| **关联** | 静态锚点 | **动态联想网络 + 预取** |
| **提炼** | 被动等待 | **主动蒸馏** |
| **强化** | 被动命中 | **间隔复习调度** |

**MAFW v6.4 已到达记忆系统的最优解——在纯文件、零外部依赖的前提下，实现了人类认知的四核深化机制。**