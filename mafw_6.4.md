基于我们全部的讨论，这是 **MAFW v6.4（认知深化 + 作用域分层架构）** 的完整、正式、可落地的最终版蓝图。

---

### 🏛️ MAFW v6.4 架构全景（最终版）

```text
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                MAFW v6.4（认知深化 + 作用域分层架构）正式版                        │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌─────────────────────────── 访问层（MCP Tools + Hooks）───────────────────────┐  │
│  │  • mafw_search（迭代检索 + 联想预取 + 自动融合项目/全局）                     │  │
│  │  • mafw_review（主动复习触发器，支持间隔调度）                               │  │
│  │  • mafw_observe（感知显著度，写入 T1）                                       │  │
│  │  • mafw_commit_heuristic（Review Agent 手动写入 L5）                        │  │
│  │  • 其他 8 个接口（保持不变）                                                 │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │              ★ 统一谐波索引与认知图（Harmonic Index + Cognitive Graph）       │  │
│  │  文件1：.mafw/memory/.harmonic_index.json（统一索引，含深化字段 + 作用域）   │  │
│  │  文件2：.mafw/memory/.cognitive_graph.json（联想权重邻接表，作用域隔离）     │  │
│  │                                                                              │  │
│  │  索引条目核心字段：                                                          │  │
│  │  • id, goal_id（项目级非空 / 全局级 null）, scope（project|global）         │  │
│  │  • primary_abstraction（6-8词）, cue_anchors（锚点数组）                    │  │
│  │  • pointer（{tier, path, record_id}）                                       │  │
│  │  • energy（0~1）, salience（0.5~1.5）                                       │  │
│  │  • review_count, last_reviewed（间隔复习）                                  │  │
│  │  • abstraction_level（0=原始, 1=叙事, 2=事实, 3=公理）                     │  │
│  │  • top_associations（联想预取指针，仅限同作用域）                           │  │
│  │  • distilled_from（蒸馏来源追溯，仅全局级有）                               │  │
│  │                                                                              │  │
│  │  ★ 检索路由规则：                                                            │  │
│  │  1. 项目级召回：强制过滤 goal_id = 当前项目，命中 T1~T4                     │  │
│  │  2. 全局级召回：用当前项目的 cue_anchors 匹配 L5（goal_id = null）          │  │
│  │  3. 融合排序：按 Energy × 匹配度 统一排序，返回时标注 [项目级]/[全局级]     │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │          记忆核心层（五层存储——严格作用域隔离 + 统一数据模型）                 │  │
│  │                                                                              │  │
│  │  ╔═══════════════════════════════════════════════════════════════════════╗  │  │
│  │  ║              【项目作用域】（Project Scope - 绑定 GoalId）            ║  │  │
│  │  ║                                                                       ║  │  │
│  │  ║  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐                ║  │  │
│  │  ║  │  T1     │  │  T2     │  │  T3     │  │  T4     │                ║  │  │
│  │  ║  │工作记忆  │  │情景记忆  │  │语义记忆  │  │过程记忆  │                ║  │  │
│  │  ║  ├─────────┤  ├─────────┤  ├─────────┤  ├─────────┤                ║  │  │
│  │  ║  │Level 0  │  │Level 1  │  │Level 2  │  │Level 2  │                ║  │  │
│  │  ║  │(原始)   │  │(叙事)   │  │(事实)   │  │(流程)   │                ║  │  │
│  │  ║  ├─────────┤  ├─────────┤  ├─────────┤  ├─────────┤                ║  │  │
│  │  ║  │❌不注册 │  │✅注册   │  │✅注册   │  │✅注册   │                ║  │  │
│  │  ║  │索引     │  │索引     │  │索引     │  │索引     │                ║  │  │
│  │  ║  ├─────────┤  ├─────────┤  ├─────────┤  ├─────────┤                ║  │  │
│  │  ║  │绑定     │  │绑定     │  │绑定     │  │绑定     │                ║  │  │
│  │  ║  │GoalId   │  │GoalId   │  │GoalId   │  │GoalId   │                ║  │  │
│  │  ║  └─────────┘  └─────────┘  └─────────┘  └─────────┘                ║  │  │
│  │  ╚═══════════════════════════════════════════════════════════════════════╝  │  │
│  │                                                                              │  │
│  │                              ★ 蒸馏桥梁                                       │  │
│  │                    （抽象蒸馏器自动运行，从项目→全局）                         │  │
│  │                              │                                                │  │
│  │                              ▼                                                │  │
│  │  ╔═══════════════════════════════════════════════════════════════════════╗  │  │
│  │  ║              【全局作用域】（Global Scope - 不绑定 GoalId）           ║  │  │
│  │  ║                                                                       ║  │  │
│  │  ║  ┌───────────────────────────────────────────────────────────────┐  ║  │  │
│  │  ║  │                         L5                                   │  ║  │  │
│  │  ║  │                   全局元认知（World Model）                   │  ║  │  │
│  │  ║  │                                                               │  ║  │  │
│  │  ║  │  存储内容：                                                   │  ║  │  │
│  │  ║  │  • 情境化启发式（Heuristics）：跨项目通用的“最佳实践”        │  ║  │  │
│  │  ║  │  • 技术公理（Axioms）：底层不可违背的铁律                    │  ║  │  │
│  │  ║  │                                                               │  ║  │  │
│  │  ║  │  Level 3（公理/铁律）                                        │  ║  │  │
│  │  ║  │  ❌ 不绑定 GoalId（跨项目共享）                              │  ║  │  │
│  │  ║  │  ✅ 注册索引，供所有项目通过锚点匹配检索                     │  ║  │  │
│  │  ║  │  ✅ 记录 distilled_from 字段，追溯来源                       │  ║  │  │
│  │  ║  └───────────────────────────────────────────────────────────────┘  ║  │  │
│  │  ╚═══════════════════════════════════════════════════════════════════════╝  │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
│  ┌──────────────────────────────────┼────────────────────────────────────────────┐  │
│  │          ★ 认知治理层（后台守护协程 - 空闲时运行，3 大引擎）                  │  │
│  │                                                                              │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ 1. 间隔复习调度器（Review Scheduler）                               │    │  │
│  │  │    • 扫描 Energy > 0.5 的记忆                                       │    │  │
│  │  │    • 计算复习间隔：1天 × 2^review_count（1→2→4→8天）              │    │  │
│  │  │    • 超期未复习 → 生成复习任务，注入上下文提示 Agent 回顾          │    │  │
│  │  │    • 复习成功后 Energy +0.05，review_count +1                      │    │  │
│  │  │    • 作用域感知：项目级和全局级分开排队                             │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ 2. 联想网络强化器（Association Builder）                            │    │  │
│  │  │    • 每次检索返回多条结果 → 两两建立连接，权重 +1                  │    │  │
│  │  │    • ★ 不跨作用域：仅连接同 GoalId（项目内）或 L5 内部（全局内）   │    │  │
│  │  │    • 周期性剪枝：移除权重 < 2 的弱连接，保持图稀疏                 │    │  │
│  │  │    • 更新 top_associations 指针，供联想预取使用                   │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐    │  │
│  │  │ ★3. 抽象蒸馏器（Abstraction Distiller）- 从项目→全局的桥梁         │    │  │
│  │  │    触发条件A（情景→语义）：                                        │    │  │
│  │  │      T2 中相同 primary_abstraction 词根 > 3 条                     │    │  │
│  │  │      → 合并为 1 条 T3 事实（Level 2），原 T2 降权至 Energy=0.4    │    │  │
│  │  │    触发条件B（流程→公理）：                                        │    │  │
│  │  │      T4 中某流程 execution_count > 5 次                            │    │  │
│  │  │      → 剥离项目特定变量（IP/密码/项目名），生成 L5 全局启发式     │    │  │
│  │  │      → 原 T4 流程 Energy 锁定为 0.4，新 L5 条目 Energy=0.7 激活   │    │  │
│  │  │    ★ 蒸馏器纯规则实现：正则剥离 + 占位符替换，不依赖 LLM          │    │  │
│  │  └─────────────────────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                     │
│  ┌─────────────────────────── 存储层（纯文件系统）──────────────────────────────┐  │
│  │                                                                              │  │
│  │  .mafw/memory/                                                               │  │
│  │  ├── .harmonic_index.json          ← ★ 全局统一索引（< 500KB）               │  │
│  │  ├── .cognitive_graph.json         ← ★ 联想邻接表（< 200KB）                │  │
│  │  ├── tier1/{goalId}/spiral-{N}.jsonl                                        │  │
│  │  ├── tier2/{goalId}.json           ← 谐波单元数组（项目级）                  │  │
│  │  ├── tier3/{goalId}.json           ← 谐波单元数组（项目级）                  │  │
│  │  ├── tier4/{goalId}.json           ← 谐波单元数组（项目级）                  │  │
│  │  └── l5/                           ← ★ 无 GoalId 子目录（全局共享）         │  │
│  │      ├── heuristics.json           ← 谐波单元数组（全局级，goal_id=null）   │  │
│  │      └── axioms.json               ← 谐波单元数组（全局级，goal_id=null）   │  │
│  │                                                                              │  │
│  └─────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                     │
│  ┌─────────────────────────── 治理层（贯穿全系统）──────────────────────────────┐  │
│  │  • Energy 系统：命中 +0.1 / 复习 +0.05 / 反馈 +0.2 / 衰减 -0.01×(1/salience) │  │
│  │  • 自动合并：MinHash 相似度 > 0.6 → 合并（仅限同作用域）                    │  │
│  │  • 乐观锁：contentHash 校验 + 重试（3 次）                                   │  │
│  │  • LLM 策略：完全可选，仅用于语义润色（Embedding/总结），非必需              │  │
│  │  • 作用域铁律：项目级（T1-T4）绝不泄露给其他项目；全局级（L5）通过锚点匹配 │  │
│  └─────────────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

### 📦 统一数据模型（谐波单元 v2.0 - 含作用域与深化字段）

```json
{
  "id": "mem_001",
  
  // ★ 作用域字段（检索路由核心）
  "goal_id": "goal_payment",        // 项目级：非空；全局级：null
  "scope": "project",               // "project" | "global"
  
  "memory_type": "semantic",        // episodic | semantic | procedural | global
  "abstraction_level": 2,           // 0=原始(T1), 1=叙事(T2), 2=事实(T3/T4), 3=公理(L5)
  
  "primary_abstraction": "支付回调幂等键设计",
  "cue_anchors": ["幂等", "Redis", "支付网关"],
  "memory_value": "使用订单号+状态机作为幂等键，Redis存储处理标记，过期24h。",
  
  // ★ 认知深化字段
  "salience": 1.5,                  // 显著度（报错/宕机=1.5, 常规=0.5）
  "energy": 0.85,
  "review_count": 3,
  "last_reviewed": "2026-07-04",
  "top_associations": ["mem_002", "mem_005"],  // 联想预取指针（同作用域）
  
  // ★ 蒸馏溯源（仅全局级有值）
  "distilled_from": ["mem_088", "mem_092"],    // 从哪些项目记忆蒸馏而来
  
  "created_at": "2026-07-01T10:00:00Z",
  "updated_at": "2026-07-05T14:20:00Z"
}
```

**联想邻接表（.cognitive_graph.json）**：
```json
{
  "edges": [
    {"source": "mem_001", "target": "mem_002", "weight": 12, "scope": "project"},
    {"source": "mem_001", "target": "mem_005", "weight": 8, "scope": "project"},
    {"source": "mem_999", "target": "mem_998", "weight": 15, "scope": "global"}
  ]
}
```

---

### 🔄 核心数据流（写入 → 蒸馏 → 检索 → 注入）

#### 1. 写入与压缩路径（含显著度感知）
```text
Agent 执行动作
    │
    ▼
PostToolUse Hook → mafw_observe
    │
    ├── 写入 T1（JSONL 追加）
    ├── ★ 感知显著度：正则扫描 Error/Crash → salience=1.5
    └── 计数器 +1
    │
    └── 满 50 条 或 Spiral 结束 → 触发混合压缩器
              │
              ├── 规则分类（免费）：提取主抽象 + 锚点
              ├── 可选 LLM 润色（非必需）
              ├── ★ 自动合并（MinHash > 0.6，仅限同作用域）
              └── 写入 T2/T3/T4（绑定 goal_id），注册索引（goal_id 非空）
```

#### 2. 抽象蒸馏路径（后台守护协程）
```text
定时扫描（每小时）：
    │
    ├── 条件A：T2 中相同词根 > 3 条
    │       → 合并为 1 条 T3 事实（Level 2）
    │       → 原 T2 降权至 Energy=0.4
    │       → 新 T3 注册索引，distilled_from 记录来源
    │
    └── 条件B：T4 中某流程 execution_count > 5 次
            → 剥离项目变量（正则替换 IP/密码/项目名）
            → 生成 L5 全局启发式（Level 3，goal_id=null）
            → 原 T4 流程 Energy 锁定为 0.4
            → 新 L5 条目 Energy=0.7，注册索引，distilled_from 记录来源
```

#### 3. 检索路径（含项目/全局融合 + 联想预取）
```text
Agent 调用 mafw_search(query)
    │
    ├── 第一轮（项目级召回）
    │   • 强制过滤 goal_id = 当前项目
    │   • 在 primary_abstraction + cue_anchors 上 BM25 匹配
    │   • 召回 Top 10
    │
    ├── 第二轮（全局级召回）
    │   • 用当前项目的 cue_anchors 匹配 L5（goal_id=null）
    │   • 召回 Top 3 全局经验
    │
    ├── 第三轮（联想预取）
    │   • 对命中的每条记忆，查 top_associations 指针
    │   • 预取关联记忆的 primary_abstraction（仅同作用域）
    │
    ├── 第四轮（融合排序）
    │   • 按 Energy × BM25分 统一降序
    │   • 返回时标注 [项目级] 或 [全局级]
    │
    └── 第五轮（网络强化）
        • 对同时命中的记忆两两建立连接，权重 +1
        • ★ 不跨作用域连接（项目内只连项目内，全局内只连全局内）
```

#### 4. 上下文注入策略（含作用域标注）
```text
<mafw_prior_knowledge>
【项目级记忆 - 当前目标】
[事实|T3|能量88%] 支付回调幂等键设计：使用订单号+状态机，Redis存储标记24h。
  → ★ 联想预取：同时加载 “网关超时配置 (关联强度92%)”

【全局级记忆 - 跨项目经验】
[全局铁律|L5|能量95%] ★ 所有支付回调必须配置指数退避重试（蒸馏自 3 个历史项目）。
</mafw_prior_knowledge>

<mafw_recent_events>
[10:32:15] 执行 npm run build 失败，报错 Cannot find module 'lodash'。
[10:32:45] 已安装 lodash，构建成功。
</mafw_recent_events>

<mafw_review_prompt>
[复习任务] 请简要回顾 “JWT并发刷新死锁” 的解决方案（上次复习：3天前）。
</mafw_review_prompt>
```

---

### 📊 完整设计原则总结（架构决策日志）

| 原则 | 说明 |
| :--- | :--- |
| **存查彻底解耦** | 五层存储只负责存 `memory_value`，检索完全依赖 `.harmonic_index.json` 统一索引。 |
| **统一数据模型** | 所有层级（T2~L5）共享同一个“谐波单元”结构，层级仅为 `memory_type` 标签和物理分区。 |
| **项目级硬隔离** | T1~T4 强制绑定 `goal_id`，检索时默认仅命中当前项目，防止跨项目幻觉。 |
| **全局级锚点匹配** | L5 不绑定 `goal_id`，通过 `cue_anchors` 按需注入，作为所有项目的“避坑指南”。 |
| **蒸馏进化桥梁** | 项目级高频经验（>3 条叙事 / >5 次流程）自动剥离变量，上升为 L5 全局公理。 |
| **显著度保护** | 报错/宕机类记忆 `salience=1.5`，衰减速度是普通记忆的 1/3，踩坑经验长期高亮。 |
| **间隔复习强化** | 按艾宾浩斯曲线调度复习（1→2→4→8天），每次成功复习 Energy+0.05。 |
| **联想网络预取** | 检索时自动预取强关联记忆（同作用域），实现触类旁通。 |
| **零向量库依赖** | 检索 = BM25 + 锚点共现 + Energy 排序，不依赖外部向量服务。 |
| **LLM 完全可选** | 核心引擎纯规则运行，LLM 仅用于语义润色（可随时关闭降级）。 |
| **纯文件 + 乐观锁** | 所有数据存 JSON/JSONL，git diff 可追溯，contentHash 乐观锁防冲突。 |
| **上下文常驻 < 4000 Tokens** | 四种注入策略协同，记忆部分仅占 ~3% 上下文预算。 |

---

### 📊 v6.3 vs v6.4 完整对比

| 维度 | v6.3（记忆存储版） | **v6.4（认知深化 + 作用域分层版）** |
| :--- | :--- | :--- |
| **作用域分层** | T1~T4 绑 GoalId，L5 不绑（静态） | 同左，但 **蒸馏器自动将项目经验上浮为全局公理** |
| **跨项目智慧沉淀** | 被动（需手动或 Review Agent 写入 L5） | **主动**（后台守护协程自动蒸馏） |
| **遗忘机制** | 固定线性衰减（-0.01/天） | **艾宾浩斯曲线** + 显著度保护（重要故障慢 3 倍） |
| **记忆连接** | 仅靠共现锚点（静态） | **动态联想网络**，检索时自动预取“死党记忆” |
| **强化手段** | 被动命中才升 Energy | **间隔复习**，主动调度提取练习 |
| **上下文内容** | 命中结果 + 热记忆预热 | 命中结果 + **联想预取** + **复习任务** + **作用域标注** |
| **存储新增** | 五层 + 索引 | 五层 + 索引 + **认知图（邻接表）** |
| **总文件大小** | ~2.5 MB / 项目 | ~3 MB / 项目（增加 < 0.5 MB） |

---

### 🛤️ 落地实施路线图（按 ROI 排序）

| 优先级 | 模块 | 改动量 | 收益 |
| :--- | :--- | :--- | :--- |
| **P0** | 作用域隔离 + 索引 `goal_id` 过滤 | ~80 行 | **必须**，防止跨项目幻觉 |
| **P0** | 显著度感知 + 衰减公式重写 | ~30 行 | 踩坑经验长期高亮 |
| **P1** | 联想预取（读 `top_associations` + 注入上下文） | ~50 行 | 触类旁通，检索体验跃升 |
| **P2** | 间隔复习调度器（后台协程 + `mafw_review` Tool） | ~150 行 | 记忆越用越深 |
| **P3** | 抽象蒸馏器（计数 + 剥离脚本 + 自动升 L5） | ~200 行 | 项目智慧自动结晶为全局公理 |

---

### ✅ 一句话总结

**v6.4 在 v6.3 的“项目/全局静态分层”基础上，修了一座“蒸馏桥”**——让每个项目里反复验证过的经验，经过显著度保护、间隔复习强化、联想网络连接后，自动结晶为全局公理，供所有后代项目继承。

这份架构现在已经完全自洽，可以作为开发 Spec 直接使用。如果你需要，我可以接着细化某个模块（比如蒸馏器的纯规则剥离脚本，或联想邻接表的剪枝算法）的伪代码实现。