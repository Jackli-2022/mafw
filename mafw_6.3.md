# MAFW 记忆系统架构演进文档（v6.0 → v6.3）

> **文档性质**：本文档完整记录了 MAFW 记忆系统从 v6.0 到 v6.3 的架构演进全过程，包含每一版本的设计目标、核心决策、问题与修正，最终收敛为 v6.3 "谐波记忆系统"的完整规格。


## 一、演进全景速览

| 版本 | 代号 | 核心主题 | 关键变化 |
| :--- | :--- | :--- | :--- |
| **v6.0** | Aligned Spiral | 愿景蓝图 | 提出自适应螺旋、记忆分层（T1-T4 + L5）、认知路由、经济感知 |
| **v6.1** | Memory Spec | 数据契约固化 | 定义五层精确 JSON Schema，明确 T1 计数规则，废弃 `session-pruner.ts` |
| **v6.2** | Index Layer | 存查解耦第一步 | 引入外部索引层（`.memory_index.json`），层级仅负责存储 |
| **v6.3** | Harmonic Memory | 深度融合 | 统一五层数据模型（谐波单元），层级降级为标签，跨层自动合并 |


## 二、v6.0 —— 愿景蓝图（Aligned Spiral）

**版本定位**：从"执行器"到"对齐器"的愿景设计，首次提出完整的五层记忆架构。

### 2.1 核心设计目标

| 目标 | 说明 |
| :--- | :--- |
| **自适应螺旋** | 将线性 Loop 升级为带回溯机制的双向螺旋（Spiral） |
| **认知路由** | 基于任务成功率和剩余预算动态选择 LLM 模型 |
| **经济可观测** | 实时 Token/成本 Telemetry，超阈值自动降级 |
| **五层记忆** | T1 工作记忆、T2 情景记忆、T3 语义记忆、T4 过程记忆、L5 待定义 |

### 2.2 五层架构（v6.0 初始定义）

| 层级 | 名称 | 职责 |
| :--- | :--- | :--- |
| T1 | 工作记忆 | 原始 Tool/File/LLM 观察，Agent 无感知自动捕获 |
| T2 | 情景记忆 | 每个 Loop 的叙事摘要，含成败原因 |
| T3 | 语义记忆 | 结构化事实（Facts）和概念（Concepts） |
| T4 | 过程记忆 | 成功的工作流模式，带成功率统计 |
| L5 | 世界模型 | **未定义**（仅预留占位） |

### 2.3 v6.0 遗留的待决策项

| # | 问题 | 状态 |
| :--- | :--- | :--- |
| 1 | 检索实现：BM25 + 向量还是纯语义搜索？ | ❌ 待定 |
| 2 | 压缩策略：全量 LLM 还是混合模式？ | ❌ 待定 |
| 3 | 隐私过滤：`mafw_observe` 是否内置脱敏？ | ❌ 待定 |
| 4 | 存储后端：纯文件还是 SQLite？ | ❌ 待定 |
| 5 | 图索引：是否构建知识图谱？ | ❌ 待定 |
| 6 | 多 Agent 并发：如何同步状态？ | ❌ 待定 |


## 三、v6.1 —— 数据契约固化（Memory Spec）

**版本定位**：对 v6.0 进行全面评审后，先固化记忆系统的数据契约（Schema），解决命名混淆和实现悬空问题。

### 3.1 解决的核心问题

| 问题 | v6.0 表现 | v6.1 修正 |
| :--- | :--- | :--- |
| **命名混淆** | "L1-L3" 与 "T1-T4" 两套体系并存 | **废弃旧命名**，统一为 T1-T4 + L5 |
| **L5 定义空白** | 仅预留占位，无实质内容 | **明确定义**为"全局元认知层（情境化启发式）" |
| **T2 叙事生成** | 未定义谁生成、何时生成 | **零成本复用**：Review Agent 的审查报告摘要直接写入 T2 |
| **T1 计数规则** | 未定义"50 条"的计数范围 | **明确按 Spiral 计数**，新 Spiral 重置计数器 |

### 3.2 L5 的最终定义（v6.1 冻结）

> **L5 是 MAFW 的全局元认知层，独立于任何具体目标（Goal），专门存储从跨项目成功/失败经验中提炼的、绑定执行上下文的决策启发式（情境化启发式），为所有 Agent 提供跨任务共享的系统级通用准则与反思性偏见。**

### 3.3 T1 计数规则（关键边界条件）

```
计数器范围：仅限 当前 Spiral
触发阈值：T1 累积 ≥ 50 条 → 立即触发混合压缩器
重启恢复：进程崩溃 → 计数器归零，不追溯历史
压缩后：标记 compressed: true，计数器重置
```

### 3.4 v6.1 遗留的架构问题

| 问题 | 表现 | 根因 |
| :--- | :--- | :--- |
| **检索器需按层路由** | 找事实去 T3，找模式去 T4，检索前必须预判类型 | 各层数据模型不同 |
| **跨层知识孤立** | "JWT 过期时间"（T3）和"修复 JWT 死锁"（T2）各存各的 | 跨层无法自动关联 |
| **压缩逻辑复杂** | 压缩器需知道"Diff 写 T4，语义写 T3，叙事写 T2" | 生成逻辑与存储目标强耦合 |
| **层级本质不清** | 层级同时承担"检索分区"和"存储分区"双重职责 | 分层设计未解耦 |


## 四、v6.2 —— 存查解耦第一步（Index Layer）

**版本定位**：在 v6.1 的五层外部包裹一层统一索引，解决"检索器需按层路由"的问题。**这是 v6.1 到 v6.3 的过渡桥梁**。

### 4.1 核心改动

```
改动前（v6.1）：
  检索器 → 判断类型 → 去对应层检索 → 返回结果
  层级 = 检索分区 + 存储分区

改动后（v6.2）：
  检索器 → 统一索引（跨层检索）→ 找到指针 → 去对应层加载内容
  层级 = 仅存储分区（检索外包给索引）
```

### 4.2 v6.2 的局限性

- **索引与数据分离**：索引只存指针，不改变各层数据结构
- **跨层合并仍不支持**：不同层的数据模型不同，无法合并
- **层级仍是数据模型的一部分**：T2 有 `episodes`，T3 有 `facts`，T4 有 `patterns`

### 4.3 v6.2 遗留下的根本矛盾

```
虽然检索层已经统一，但写入层仍需要知道：
  "这条数据是事实，要去 T3 写；那条是叙事，要去 T2 写"

→ 压缩逻辑仍然耦合存储目标
→ 跨层知识仍然无法自动合并
```


## 五、v6.3 —— 谐波记忆系统（Harmonic Memory）

**版本定位**：彻底重构五层的数据内核，从"分区路由表"进化为"共享知识图谱"。**这是 MAFW + Memora 深度融合的最终形态。**

### 5.1 核心洞察

> **不改目录结构，不改存储介质，不改 API 接口——只改五层内部每条记录的 JSON 形态，让它们从"不同类型的内容"变为"同一种谐波单元的不同标签"。**

### 5.2 统一数据模型：谐波记忆单元

```json
{
  "id": "mem_xxx",
  "goal_id": "goal_123" | null,              // L5 为 null
  "memory_type": "episodic|semantic|procedural|global", // 原层级降级为标签
  "primary_abstraction": "6-8词核心摘要",     // ★ 统一索引键
  "cue_anchors": ["锚点1", "锚点2"],          // ★ 多跳线索
  "memory_value": "完整的详细内容...",         // ★ 真正存储的信息
  "energy": 0.75,
  "created_at": "...",
  "updated_at": "...",
  "merged_from": ["old_id1", "old_id2"]       // ★ 自动合并时记录来源
}
```

### 5.3 层级的最终定位

| 维度 | v6.1（原版） | v6.3（谐波版） |
| :--- | :--- | :--- |
| **五层数据模型** | 各不相同 | **完全统一（谐波单元）** |
| **层级本质** | 检索分区 + 存储分区 | **仅存储分区，层级=标签** |
| **跨层合并** | 不支持 | **支持（同模型，可跨层）** |
| **检索逻辑** | 按层路由 | **完全无视层级，只认索引** |
| **`memory_type` 角色** | 路由键 | **元数据标签（仅提示）** |

### 5.4 目录结构（v6.3 最终形态）

```
.mafw/memory/
├── .harmonic_index.json          # ★ 全局统一索引（内存常驻）
├── tier1/{goalId}/spiral-{N}.jsonl  # 不变：原始观察缓冲区
├── tier2/{goalId}.json           # ★ 谐波单元数组（memory_type="episodic"）
├── tier3/{goalId}.json           # ★ 谐波单元数组（memory_type="semantic"）
├── tier4/{goalId}.json           # ★ 谐波单元数组（memory_type="procedural"）
└── l5/
    ├── heuristics.json           # ★ 谐波单元数组（memory_type="global"）
    └── axioms.json               # ★ 谐波单元数组（memory_type="global"）
```

### 5.5 三大能力升级

#### 5.5.1 检索：无视层级的纯索引操作

```
mafw_search(query)
     │
     ▼
Step 1: 统一 BM25 扫描（.harmonic_index.json 内存倒排）
     │  在 primary_abstraction + cue_anchors 上匹配
     ▼
Step 2: 锚点共现扩展
     │  提取高频锚点 → 二次扫描索引
     ▼
Step 3: 按需加载
     │  根据指针去对应 Tier 加载 memory_value
     │  附上 memory_type 标签（仅作上下文提示）
```

**关键点**：检索器**完全不关心** `memory_type` 是什么。

#### 5.5.2 自动合并：跨层知识蒸馏

```
场景：T3 事实 "JWT 过期时间设为 30 分钟"
     + T2 叙事 "修复 JWT 刷新并发死锁"

压缩器检测到 primary_abstraction 相似 > 0.6
     │
     ▼
合并后：
  primary_abstraction: "JWT 过期 30 分钟 + 修复刷新死锁"
  cue_anchors: ["JWT", "过期时间", "刷新", "并发死锁"]
  memory_value: "JWT 过期时间设为 30 分钟\n[关联] 修复并发死锁"
  merged_from: ["old_T3_id", "old_T2_id"]
  energy += 0.15
```

#### 5.5.3 压缩器：统一生成谐波单元

```
混合压缩器处理完原始观察后：
  1. 生成 primary_abstraction（规则提取关键词 + 动宾结构，无需 LLM）
  2. 提取 cue_anchors（实体/技术栈，正则 + 词频）
  3. 生成 memory_value（完整结构化内容）
  4. 打上 memory_type 标签（Diff→procedural，Plan→episodic，Fact→semantic）
  5. ★ 写入对应 Tier（所有记录结构一致）
  6. ★ 更新 .harmonic_index.json
  7. ★ 触发跨层合并检查（MinHash 相似度 > 0.6）
```


## 六、关键技术决策：从 v6.0 到 v6.3 的演变

| 决策点 | v6.0（初始） | v6.1 | v6.2 | v6.3（最终） |
| :--- | :--- | :--- | :--- | :--- |
| **向量库** | 待定（计划引入） | 待定 | 可选 | **不需要**（BM25 + 锚点共现） |
| **并发控制** | 待定（向量时钟候选） | 待定 | 乐观锁候选 | **乐观锁（contentHash + 重试）** |
| **存储后端** | 待定（文件/SQLite） | 纯文件 | 纯文件 | **纯文件（JSON/JSONL）** |
| **LLM 角色** | 核心（压缩/检索） | 核心 | 核心 | **完全可选（仅润色层）** |
| **图数据库** | 待定（Neo4j 候选） | 明确砍掉 | 明确砍掉 | **不需要（LLM 自然语言推理）** |
| **Checkpoint** | 未定义 | Git-based | Git-based | **Git-based（放弃 tar.gz）** |
| **数据模型** | 各层不同 | 各层不同 | 各层不同 | **统一（谐波单元）** |
| **层级角色** | 检索 + 存储 | 检索 + 存储 | 仅存储 | **仅存储（层级=标签）** |


## 七、演进图：从 v6.0 到 v6.3 的路径

```
v6.0 愿景蓝图
    │
    │  设计评审 → 发现 12 个架构问题
    ▼
v6.1 数据契约固化
    │  • 统一命名（T1-T4 + L5）
    │  • L5 明确定义
    │  • T1 计数规则锁定
    │  • T2 零成本复用
    │
    │  遗留问题：检索需按层路由、跨层知识孤立
    ▼
v6.2 外部索引层
    │  • 引入 .memory_index.json
    │  • 存查解耦第一步
    │
    │  遗留问题：写入仍耦合存储目标、数据模型仍各层不同
    ▼
v6.3 谐波记忆系统
    │  • 统一五层数据模型
    │  • 层级降级为标签
    │  • 跨层自动合并
    │  • 检索完全无视层级
    │
    ▼
🔒 设计冻结，进入开发
```


## 八、v6.3 最终架构总结

> **MAFW v6.3 = 五层物理分区（T1-T4 + L5）+ 一套逻辑模型（谐波单元）+ 一个统一索引（harmonic_index.json）+ 三种纯本地机制（BM25 + 锚点共现 + MinHash）**

### 8.1 核心能力矩阵

| 能力 | 实现机制 | 是否依赖向量 | 是否依赖 LLM |
| :--- | :--- | :--- | :--- |
| **精确关键词召回** | BM25 倒排索引 | ❌ | ❌ |
| **同义/近义召回** | L5 同义词表 → 查询扩展 | ❌ | ❌（手动维护） |
| **多跳推理** | cue_anchors 高频共现 → 二次扫描 | ❌ | ❌ |
| **跨层自动合并** | MinHash + Jaccard（> 0.6） | ❌ | ❌ |
| **记忆热度排序** | Energy × BM25 原始分 | ❌ | ❌ |
| **语义润色** | 可选 LLM 调用（非必需） | ❌ | ✅（可选） |

### 8.2 规模预估

| 指标 | v6.1 | v6.3 |
| :--- | :--- | :--- |
| **单项目全量记忆** | ~1-2 MB | ~1-2 MB（无变化） |
| **统一索引文件** | 无（检索分散在各层） | < 500 KB（内存常驻） |
| **检索延迟** | 依赖层级遍历 | < 5ms（纯内存倒排） |
| **月均 LLM 成本** | 压缩+检索双高 | < $5（仅可选润色层） |


## 九、开发入口：从哪开始？

根据这份演进文档，开发顺序建议如下：

| 阶段 | 任务 | 输入 | 输出 | 工期 |
| :--- | :--- | :--- | :--- | :--- |
| **M1** | 数据模型 + 索引读写器 | 谐波单元 Schema | `harmonic_index.json` CRUD | 2 天 |
| **M2** | 迁移脚本（v6.1 → v6.3） | 旧 T2/T3/T4/L5 格式 | 新格式数据 | 1 天 |
| **M3** | BM25 检索器 | `.harmonic_index.json` | `mafw_search` 实现 | 3 天 |
| **M4** | 压缩器改写 | T1 原始观察 | 谐波单元生成 + 索引更新 | 3 天 |
| **M5** | MinHash 合并 | 新谐波单元 | 跨层合并逻辑 | 2 天 |
| **M6** | 集成测试 | 端到端流程 | 验收通过 | 2 天 |

**总工期预估**：13 个工作日（约 3 周）


**结语**：从 v6.0 的愿景蓝图到 v6.3 的谐波记忆系统，MAFW 的记忆架构经历了三次关键迭代——**固化契约、存查解耦、深度融合**。每一版都解决了前一版的核心矛盾，最终收敛为一个 **纯文件、无向量、Git 原生、零外部依赖** 的轻量级记忆系统，同时保留了 Memora 理论中"存查彻底解耦、跨层自动合并"的核心思想。

文档完备。可以进入 M1 开发阶段。🚀